server:
  port: 8761

spring:
  application:
    name: spring-cloud-eureka
  profiles:
    active: dev 

  cloud:
    inetutils:
      ignoredInterfaces:
        - docker0
        - veth.*
        - VM.*
      preferredNetworks:
        - 192.168

  security:
    user:
      name: root
      password: passw0rd